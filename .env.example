# Copia este archivo a .env y ajusta los valores. NUNCA subas .env real al repo.

# Entorno de la app: local | staging | production
APP_ENV=local

# Conexión a PostgreSQL (usada por el servicio api)
DATABASE_URL=postgresql+psycopg://labx:labx@postgres:5432/labx
